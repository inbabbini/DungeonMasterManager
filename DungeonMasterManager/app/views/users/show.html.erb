<div class="row justify-content-center">
	<div class="col-6">
		<legend> My Information </legend>
		<div>
				<%= image_tag @user.picture.url(:normal), class: "img-thumbnail rounded mx-auto d-block" %>
		</div>
		<ul class="list-group">
			<li class="list-group-item">
				<b>Name:</b> <%= @user.name %>
			</li>
			<li class="list-group-item">
				<b>Mail:</b> <%= @user.email %>
			</li>
			<li class="list-group-item">
				<b>Registration Date: </b> <%= time_tag(@user.created_at) %>
			</li>
		</ul>
		<%= link_to 'Edit Account', edit_user_path(@user), class: "btn btn-sm btn-primary text-light" %>
		<%= link_to 'Delete Account', user_path(@user), class: "btn btn-sm btn-danger text-light", method: :delete, data: { confirm: 'Are you sure?' } %>
	</div>
</div>
