<p id="notice"><%= notice %></p>

<h1>Notes</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @notes.each do |note| %>
      <tr>
        <td><%= note.title %></td>
        <td><%= note.content %></td>
        <td><%= link_to 'Show', game_character_note_path(note.character.game, note.character, note) %></td>
        <td><%= link_to 'Edit', edit_game_character_note_path(note.character.game, note.character, note) %></td>
        <td><%= link_to 'Destroy', game_character_note_path(note.character.game, note.character, note), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Note', new_game_character_note_path %>
